{
  "name": "Training",
  "version": "1.0.0",
  "description": "Training project",
  "main": "index.js",
  "scripts": {
    "start": "lite-server --baseDir=src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch:scss": "sass --watch src/styles/main.scss src/styles/style.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^5.3.2"
  },
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
